---
import type { THeaderItemId, THeaderItem } from '@contracts';

const baseURL = import.meta.env.PROD ? `${import.meta.env.SITE}/` : import.meta.env.BASE_URL;

const navItems: THeaderItem[] = [
  { id: 'home', text: '_home', href: baseURL },
  { id: 'about', text: '_about-me', href: `${baseURL}about` },
  { id: 'projects', text: '_projects', href: `${baseURL}projects` }
];

interface Props {
  active?: THeaderItemId;
}

const { active = 'home' } = Astro.props;
---

<header
  class="border-b-big-stone z-50 flex max-h-14 min-h-14 w-full items-center border-b bg-transparent font-mono text-[#607B96] no-underline"
>
  <h4
    class="border-r-big-stone flex h-full w-64 items-center border-r pl-6 text-base font-semibold animate-duration-500 animate-ease-linear"
  >
    <a
      href="/"
      class="flex min-w-60 animate-fade-right items-center gap-2 transition-all duration-300 animate-duration-500 animate-ease-linear hover:text-jagc-400"
    >
      <img src="/favicon.svg" alt="Logo" class="h-9" alt="JAGC Logo" />
      jaime-gutierrez
    </a>
  </h4>

  <nav class="flex h-full flex-1 animate-fade-left animate-duration-500 animate-ease-linear">
    {
      navItems.map(({ id, href, text }) => (
        <a
          href={href}
          class:list={[
            'border-r-big-stone flex h-full items-center border-r px-2 text-lg font-medium no-underline transition-all duration-300 hover:bg-[#011221] hover:text-jagc-400',
            { 'border-b-2 border-b-jagc-500 text-jagc-400': id === active }
          ]}
        >
          {text}
        </a>
      ))
    }

    <span class="flex flex-1 justify-end">
      <a
        href={`${baseURL}contact`}
        class:list={[
          'border-l-big-stone flex h-full items-center border-l pl-6 pr-6 text-lg font-medium no-underline transition-all duration-300 hover:bg-[#011221] hover:text-jagc-400',
          { 'border-b-2 border-b-jagc-500 text-jagc-400': active === 'contact' }
        ]}>_contact-me</a
      >
    </span>
  </nav>
</header>
